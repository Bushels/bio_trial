# 2026-05

## 2026-05-01 MPS Portfolio Cleanup

Ran the MPS portfolio cleanup workflow per `MPS/docs/templates/portfolio-cleanup-prompt.md`. Codex drafted the doc artifacts and the cleanup audit; Claude executed file moves, gitignore patching, and commits because Codex's sandbox identity hit Windows ACL deny on `.git/` and `.codex/`.

### Commits shipped (9)

1. `chore(gitignore): unblock .claude/agents and .claude/skills tracking, anchor screenshot ignores`
2. `chore(scaffold): add .claude/agents and .claude/skills dirs`
3. `docs(rules): add AGENTS.md and CLAUDE.md`
4. `docs(state): add PROJECT_STATE.md`
5. `chore(codex): scaffold .codex/config.toml with gpt-5.5`
6. `chore(media): relocate Biostim.jpg + verification screenshots into video tree`
7. `chore(video): track video/ and remotion-video/ source trees with per-dir gitignore`
8. `docs(inquiries): track docs/inquiries/ seed-rep scenario notes`
9. `docs(journal): record 2026-05 portfolio cleanup`

### Files moved into `video/`

To make the Remotion reel self-contained (matching the `remotion-video/public/assets/` pattern):

- `Biostim.jpg` → `video/Biostim.jpg` (`git mv`)
- `crops-view.png` → `video/crops-view.png` (plain mv; was untracked at root)
- `verify-desktop-trial-desk.png` → `video/verify-desktop-trial-desk.png`
- `verify-mobile-trial-desk.png` → `video/verify-mobile-trial-desk.png`

Imports in `video/src/FarmerExplainerReel.jsx` lines 16–19 were updated from `"../../FILE"` to `"../FILE"`.

### Files deleted (working-tree only; all `.gitignore`-matched, zero commit impact)

- `after-removal.png`
- `desktop-full.png`
- `mobile-full.png`
- `eadf612ecb81` (orphan Gemini export artifact)

### Skill promotions

Zero. No global writes to `~/.claude/skills/`. Future candidates noted for later passes:

- Bio Trial Supabase Edge Function verification checklist
- Static preview / CSS inspection workflow keyed off `.claude/launch.json`
- Telegram farmer workflow test checklist

### Sandbox notes

Codex's restricted user identity hit DENY ACEs on `.git/` and `.codex/` — same pattern as the wellfi-app blocker the cleanup template warns about. Resolved per-step from outside the sandbox via Claude's Bash tool (write `.codex/config.toml`, delete the four cruft files, run all `git` operations, push). Future runs of this template against bio_trial should not need the workaround if the sandbox identity is granted full control of `.git/` and `.codex/`.

### Pending follow-ups

None from this cleanup. Open product plans live in `docs/plans/`.
